# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (MongoDB Atlas Cloud or Local)
MONGODB_URI=mongodb+srv://<username>:<password>@esquare.bwb5zi0.mongodb.net/esquare_ott?retryWrites=true&w=majority&appName=ESquare


# JWT Authentication
JWT_SECRET=esquare_ott_super_secure_jwt_secret_key_2026_x!
JWT_EXPIRES_IN=7d

# OTP Settings (Always generates a fresh random 4-digit code)
OTP_COOLDOWN_SECONDS=60
OTP_EXPIRY_SECONDS=300
MAX_OTP_ATTEMPTS=5

# CORS Allowed Origins (Comma-separated)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173
